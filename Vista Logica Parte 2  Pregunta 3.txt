//Parte 2

//Vista lógica 3
//Cuantos buses operaron cada hora

Create View Buses

AS

SELECT Datepart(hour, fecha_transaccion) as hora,
       count(Bus) as buses
FROM MIBUS.dbo.transacciones
GROUP BY Datepart(hour, fecha_transaccion)
ORDER BY Datepart(hour, fecha_transaccion)


